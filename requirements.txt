fastapi==0.141.1
uvicorn==0.52.1
# Process manager for production: runs MULTIPLE uvicorn workers.
# Linux/Docker only (won't execute on Windows) - used in the prod container CMD.
gunicorn==23.0.0
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
python-jose[cryptography]==3.5.0
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.32
email-validator==2.3.0
python-dotenv==1.2.2
pydantic-settings==2.14.2
alembic==1.19.1
redis
# Development / Testing
pytest
httpx
