# Used automatically by Vite during `npm run build` (production builds).
# "/api" = same-origin relative path: the browser sends API calls to the
# SAME host serving the React app; nginx proxies /api/* to FastAPI.
# Same origin = CORS never triggers in production.
VITE_API_BASE_URL=/api
