import os from 'node:os';
import path from 'node:path';
export const REPOMIX_TMP_DIR_NAME = 'repomix';
export const getRepomixTmpDir = () => {
    return path.join(os.tmpdir(), REPOMIX_TMP_DIR_NAME);
};
