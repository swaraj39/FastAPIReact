import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
export interface McpServerConfig {
    sandboxed: boolean;
    root: string;
}
export declare const createMcpServer: (config?: McpServerConfig) => Promise<McpServer>;
type RunMcpServerOptions = {
    sandboxed?: boolean;
    root?: string;
    processExit?: (code?: number) => never;
};
export declare const runMcpServer: (options?: RunMcpServerOptions) => Promise<void>;
export {};
