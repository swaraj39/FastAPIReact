import type { FileTokenInfo } from './types.js';
export interface FileWithTokens {
    path: string;
    tokens: number;
}
export interface TokenCountTreeNode {
    files: FileTokenInfo[];
    tokenSum: number;
    children: Map<string, TokenCountTreeNode>;
}
export declare const buildTokenCountTree: (filesWithTokens: FileWithTokens[]) => TokenCountTreeNode;
