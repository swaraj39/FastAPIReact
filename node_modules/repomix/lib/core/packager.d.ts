import type { RepomixConfigMerged } from '../config/configSchema.js';
import type { RepomixProgressCallback } from '../shared/types.js';
import { collectFiles, type SkippedFileInfo } from './file/fileCollect.js';
import { sortPaths } from './file/filePathSort.js';
import { processFiles } from './file/fileProcess.js';
import { applyFileProcessors } from './file/fileProcessorRun.js';
import { searchFiles } from './file/fileSearch.js';
import type { ProcessedFile } from './file/fileTypes.js';
import { getGitDiffs } from './git/gitDiffHandle.js';
import { getGitLogs } from './git/gitLogHandle.js';
import { calculateMetrics, createMetricsTaskRunner } from './metrics/calculateMetrics.js';
import { prefetchSortData, sortOutputFiles } from './output/outputSort.js';
import { produceOutput } from './packager/produceOutput.js';
import type { SuspiciousFileResult } from './security/securityCheck.js';
import { validateFileSafety } from './security/validateFileSafety.js';
import type { PackSkillParams } from './skill/packSkill.js';
export interface PackResult {
    totalFiles: number;
    totalCharacters: number;
    totalTokens: number;
    fileCharCounts: Record<string, number>;
    fileTokenCounts: Record<string, number>;
    gitDiffTokenCount: number;
    gitLogTokenCount: number;
    outputFiles?: string[];
    suspiciousFilesResults: SuspiciousFileResult[];
    suspiciousGitDiffResults: SuspiciousFileResult[];
    suspiciousGitLogResults: SuspiciousFileResult[];
    processedFiles: ProcessedFile[];
    safeFilePaths: string[];
    skippedFiles: SkippedFileInfo[];
}
declare const defaultDeps: {
    searchFiles: typeof searchFiles;
    collectFiles: typeof collectFiles;
    applyFileProcessors: typeof applyFileProcessors;
    processFiles: typeof processFiles;
    validateFileSafety: typeof validateFileSafety;
    produceOutput: typeof produceOutput;
    calculateMetrics: typeof calculateMetrics;
    createMetricsTaskRunner: typeof createMetricsTaskRunner;
    sortPaths: typeof sortPaths;
    sortOutputFiles: typeof sortOutputFiles;
    prefetchSortData: typeof prefetchSortData;
    getGitDiffs: typeof getGitDiffs;
    getGitLogs: typeof getGitLogs;
    packSkill: (params: PackSkillParams) => Promise<PackResult>;
};
export interface PackOptions {
    skillName?: string;
    skillDir?: string;
    skillProjectName?: string;
    skillSourceUrl?: string;
    confineToBaseDir?: boolean;
}
export declare const pack: (rootDirs: string[], config: RepomixConfigMerged, progressCallback?: RepomixProgressCallback, overrideDeps?: Partial<typeof defaultDeps>, explicitFiles?: string[], options?: PackOptions) => Promise<PackResult>;
export {};
