import path from 'node:path';
import { logger } from '../shared/logger.js';
import { logMemoryUsage, withMemoryLogging } from '../shared/memoryUtils.js';
import { collectFiles } from './file/fileCollect.js';
import { resolveFileLevel } from './file/fileLevelResolve.js';
import { sortPaths } from './file/filePathSort.js';
import { processFiles } from './file/fileProcess.js';
import { applyFileProcessors } from './file/fileProcessorRun.js';
import { searchFiles } from './file/fileSearch.js';
import { getGitDiffs } from './git/gitDiffHandle.js';
import { getGitLogs } from './git/gitLogHandle.js';
import { calculateMetrics, createMetricsTaskRunner } from './metrics/calculateMetrics.js';
import { loadTokenCountCache, saveTokenCountCache } from './metrics/tokenCountCache.js';
import { prefetchSortData, sortOutputFiles } from './output/outputSort.js';
import { produceOutput } from './packager/produceOutput.js';
import { buildFileDisplayPath, buildRootLabels, usesRootLabels } from './packager/rootDisplayPath.js';
import { validateFileSafety } from './security/validateFileSafety.js';
const defaultDeps = {
    searchFiles,
    collectFiles,
    applyFileProcessors,
    processFiles,
    validateFileSafety,
    produceOutput,
    calculateMetrics,
    createMetricsTaskRunner,
    sortPaths,
    sortOutputFiles,
    prefetchSortData,
    getGitDiffs,
    getGitLogs,
    packSkill: async (params) => {
        const { packSkill } = await import('./skill/packSkill.js');
        return packSkill(params);
    },
};
export const pack = async (rootDirs, config, progressCallback = () => { }, overrideDeps = {}, explicitFiles, options = {}) => {
    const deps = {
        ...defaultDeps,
        ...overrideDeps,
    };
    logMemoryUsage('Pack - Start');
    const tokenCacheLoadPromise = loadTokenCountCache();
    const sortDataPromise = deps.prefetchSortData(config).catch((error) => {
        logger.trace('Failed to prefetch sort data:', error);
    });
    progressCallback('Searching for files...');
    const searchResultsByDir = await withMemoryLogging('Search Files', async () => Promise.all(rootDirs.map(async (rootDir) => {
        const result = await deps.searchFiles(rootDir, config, explicitFiles, options.confineToBaseDir);
        return { rootDir, filePaths: result.filePaths, emptyDirPaths: result.emptyDirPaths };
    })));
    const filePathStyle = config.output.filePathStyle;
    const rootLabels = usesRootLabels(filePathStyle) && rootDirs.length > 1 ? buildRootLabels(rootDirs, config.cwd) : undefined;
    const emptyDirPaths = config.output.includeEmptyDirectories
        ? [
            ...new Set(searchResultsByDir.flatMap(({ rootDir, emptyDirPaths }, index) => {
                const rootLabel = rootLabels?.[index];
                return emptyDirPaths.map((emptyDirPath) => buildFileDisplayPath({
                    rootDir,
                    filePath: emptyDirPath,
                    cwd: config.cwd,
                    filePathStyle,
                    rootLabel,
                }));
            })),
        ].sort()
        : undefined;
    progressCallback('Sorting files...');
    const sortedFilePathsByDir = searchResultsByDir.map(({ rootDir, filePaths }) => ({
        rootDir,
        filePaths: deps.sortPaths([...new Set(filePaths)]),
    }));
    const displayFilePathsByDir = sortedFilePathsByDir.map(({ rootDir, filePaths }, index) => {
        const rootLabel = rootLabels?.[index];
        return {
            rootDir,
            filePaths: filePaths.map((filePath) => buildFileDisplayPath({
                rootDir,
                filePath,
                cwd: config.cwd,
                filePathStyle,
                rootLabel,
            })),
        };
    });
    const allFilePaths = displayFilePathsByDir.flatMap(({ filePaths }) => filePaths);
    const { taskRunner: metricsTaskRunner, warmupPromise: metricsWarmupPromise } = deps.createMetricsTaskRunner(rootDirs, allFilePaths.length, config.tokenCount.encoding);
    try {
        progressCallback('Collecting files...');
        const [collectResults, gitDiffResult, gitLogResult] = await Promise.all([
            withMemoryLogging('Collect Files', async () => await Promise.all(sortedFilePathsByDir.map(async ({ rootDir, filePaths }) => {
                const collected = await deps.collectFiles(filePaths, rootDir, config, progressCallback);
                const rawFiles = await deps.applyFileProcessors(collected.rawFiles, rootDir, config, progressCallback);
                return { ...collected, rawFiles };
            }))),
            deps.getGitDiffs(rootDirs, config),
            deps.getGitLogs(rootDirs, config),
        ]);
        const rawFiles = collectResults.flatMap((curr, index) => {
            const rootDir = sortedFilePathsByDir[index]?.rootDir;
            if (!rootDir)
                return [];
            const rootLabel = rootLabels?.[index];
            return curr.rawFiles.map((file) => ({
                ...file,
                level: resolveFileLevel(file.path, config.output),
                path: buildFileDisplayPath({
                    rootDir,
                    filePath: file.path,
                    cwd: config.cwd,
                    filePathStyle,
                    rootLabel,
                }),
            }));
        });
        const allSkippedFiles = collectResults.flatMap((curr, index) => {
            const rootDir = sortedFilePathsByDir[index]?.rootDir;
            if (!rootDir)
                return [];
            const rootLabel = rootLabels?.[index];
            return curr.skippedFiles.map((file) => ({
                ...file,
                path: buildFileDisplayPath({
                    rootDir,
                    filePath: file.path,
                    cwd: config.cwd,
                    filePathStyle,
                    rootLabel,
                }),
            }));
        });
        const [validationResult, allProcessedFiles] = await Promise.all([
            withMemoryLogging('Security Check', () => deps.validateFileSafety(rawFiles, progressCallback, config, gitDiffResult, gitLogResult)),
            withMemoryLogging('Process Files', () => {
                progressCallback('Processing files...');
                return deps.processFiles(rawFiles, config, progressCallback);
            }),
        ]);
        const { safeFilePaths, suspiciousFilesResults, suspiciousGitDiffResults, suspiciousGitLogResults } = validationResult;
        const suspiciousPathSet = new Set(suspiciousFilesResults.map((r) => r.filePath));
        const filteredProcessedFiles = suspiciousPathSet.size > 0 ? allProcessedFiles.filter((f) => !suspiciousPathSet.has(f.path)) : allProcessedFiles;
        await sortDataPromise;
        const processedFiles = await deps.sortOutputFiles(filteredProcessedFiles, config);
        progressCallback('Generating output...');
        if (config.skillGenerate !== undefined && options.skillDir) {
            const result = await deps.packSkill({
                rootDirs,
                config,
                options,
                processedFiles,
                allFilePaths,
                gitDiffResult,
                gitLogResult,
                suspiciousFilesResults,
                suspiciousGitDiffResults,
                suspiciousGitLogResults,
                safeFilePaths,
                skippedFiles: allSkippedFiles,
                progressCallback,
            });
            logMemoryUsage('Pack - End');
            return result;
        }
        const filePathsByRoot = usesRootLabels(filePathStyle)
            ? sortedFilePathsByDir.map(({ rootDir, filePaths }, index) => ({
                rootLabel: rootLabels?.[index] ?? (path.basename(rootDir) || rootDir),
                files: filePaths,
            }))
            : undefined;
        await metricsWarmupPromise;
        await tokenCacheLoadPromise;
        const outputPromise = deps.produceOutput(rootDirs, config, processedFiles, allFilePaths, gitDiffResult, gitLogResult, progressCallback, filePathsByRoot, emptyDirPaths);
        const outputForMetricsPromise = outputPromise.then((r) => r.outputForMetrics);
        const [{ outputFiles }, metrics] = await Promise.all([
            outputPromise,
            withMemoryLogging('Calculate Metrics', () => deps.calculateMetrics(processedFiles, outputForMetricsPromise, progressCallback, config, gitDiffResult, gitLogResult, {
                taskRunner: metricsTaskRunner,
            })),
        ]);
        const result = {
            ...metrics,
            ...(outputFiles && { outputFiles }),
            suspiciousFilesResults,
            suspiciousGitDiffResults,
            suspiciousGitLogResults,
            processedFiles,
            safeFilePaths,
            skippedFiles: allSkippedFiles,
        };
        await saveTokenCountCache(rootDirs);
        logMemoryUsage('Pack - End');
        return result;
    }
    finally {
        await metricsWarmupPromise.catch(() => { });
        await metricsTaskRunner.cleanup();
    }
};
