import type { RepomixConfigMerged } from '../../../config/configSchema.js';
import type { FileInclusionLevel } from '../fileLevelResolve.js';
import type { ProcessedFile, RawFile } from '../fileTypes.js';
export interface FileProcessTask {
    rawFile: RawFile;
    config: RepomixConfigMerged;
    level: FileInclusionLevel;
}
export default _default;
declare function _default({ rawFile, config, level }: FileProcessTask): Promise<ProcessedFile>;
export declare const onWorkerTermination: () => Promise<void>;
