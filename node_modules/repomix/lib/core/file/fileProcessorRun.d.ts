import { execFile } from 'node:child_process';
import type { RepomixConfigMerged } from '../../config/configSchema.js';
import type { RepomixProgressCallback } from '../../shared/types.js';
import type { RawFile } from './fileTypes.js';
export declare const DEFAULT_FILE_PROCESSOR_TIMEOUT_MS = 60000;
export interface RunProcessorCommandParams {
    command: string;
    content: string;
    tempFilePath: string;
    timeout: number;
    cwd: string;
}
export declare const runProcessorCommand: (params: RunProcessorCommandParams, deps?: {
    execFileAsync: typeof execFile.__promisify__;
}) => Promise<string>;
export declare const applyFileProcessors: (rawFiles: RawFile[], rootDir: string, config: RepomixConfigMerged, progressCallback?: RepomixProgressCallback, deps?: {
    runProcessorCommand: typeof runProcessorCommand;
}) => Promise<RawFile[]>;
export declare const logFileProcessorStatus: (config: RepomixConfigMerged) => void;
