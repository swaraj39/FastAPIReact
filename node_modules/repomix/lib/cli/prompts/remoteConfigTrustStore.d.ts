import fs from 'node:fs/promises';
export declare const sha256: (value: string | Buffer) => string;
export declare const isRemoteConfigTrusted: (repoUrl: string, configDigest: string, deps?: {
    lstat: typeof fs.lstat;
    readFile: typeof fs.readFile;
}) => Promise<boolean>;
export declare const markRemoteConfigTrusted: (repoUrl: string, configDigest: string, deps?: {
    mkdir: typeof fs.mkdir;
    chmod: typeof fs.chmod;
    writeFile: typeof fs.writeFile;
    lstat: typeof fs.lstat;
}) => Promise<void>;
