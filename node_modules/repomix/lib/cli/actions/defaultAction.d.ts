import { type RepomixConfigCli, type RepomixConfigMerged } from '../../config/configSchema.js';
import { type PackResult } from '../../core/packager.js';
import type { RepomixProgressCallback } from '../../shared/types.js';
import type { CliOptions } from '../types.js';
export interface DefaultActionRunnerResult {
    packResult: PackResult;
    config: RepomixConfigMerged;
}
export declare const buildMergedConfig: (cwd: string, cliOptions: CliOptions) => Promise<RepomixConfigMerged>;
export declare const runDefaultAction: (directories: string[], cwd: string, cliOptions: CliOptions, progressCallback?: RepomixProgressCallback) => Promise<DefaultActionRunnerResult>;
export declare const buildCliConfig: (options: CliOptions) => RepomixConfigCli;
